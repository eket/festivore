// Generated by CoffeeScript 1.3.3
(function() {
  var App, app,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  $(function() {
    var socket;
    socket = window.socket = io.connect();
    socket.on('connect', function() {
      console.log('connect');
      return socket.emit('auth', 'brainrape', function(username) {
        return console.log('auth reply', username);
      });
    });
    socket.on('update', function(update) {
      return console.log('update', update);
    });
    return socket.on('welcome', function() {
      return console.log('welcome');
    });
  });

  App = (function(_super) {

    __extends(App, _super);

    function App() {
      return App.__super__.constructor.apply(this, arguments);
    }

    App.prototype.routes = {
      "/": "map",
      "/friends": "friends",
      "/friends/add": "add_friend",
      "/friends/:friend": "friend",
      "/places": "categories",
      "/places/:category": "places",
      "/places/:category/:place": "place",
      "/places/:category/:place/edit": "edit_place",
      "/places/add": "add_place"
    };

    App.prototype.map = function() {};

    App.prototype.friends = function() {};

    App.prototype.add_friend = function() {};

    App.prototype.friend = function() {};

    App.prototype.categories = function() {};

    App.prototype.places = function() {};

    App.prototype.place = function() {};

    App.prototype.edit_place = function() {};

    App.prototype.add_place = function() {};

    return App;

  })(Backbone.Router);

  app = new App();

  Backbone.history.start();

}).call(this);
